import Layout from "../components/layout/Layout";
import SEO from "../components/seo/SEO";

# A Markdown Page

This is an example of writing a page (component) in markdown.

-   A list item
-   Another list item

export default ({ children }) => (
    <Layout>
        <SEO title="An MDX Page" />
        {children}
    </Layout>
);
